name: flutter_unionad
description: 字节跳动 穿山甲广告Flutter版
version: 2.2.3
homepage: https://github.com/gstory0404/flutter_unionad

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.gstory.flutter_unionad
        pluginClass: FlutterUnionadPlugin
      ios:
        pluginClass: FlutterUnionadPlugin
      ohos:
        pluginClass: FlutterUnionadPlugin
